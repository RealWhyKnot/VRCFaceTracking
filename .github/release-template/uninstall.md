## Uninstall

Close VRCFaceTracking and delete the extracted folder. Settings and modules stay in `%AppData%\VRCFaceTracking` and logs in `%LocalAppData%\VRCFaceTracking\logs`; delete those two folders too for a clean removal.
