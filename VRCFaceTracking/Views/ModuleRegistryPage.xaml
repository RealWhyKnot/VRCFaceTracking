<Page
    x:Class="VRCFaceTracking.Views.ModuleRegistryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:VRCFaceTracking.Core.Models"
    xmlns:views="using:VRCFaceTracking.Views"
    xmlns:behaviors="using:VRCFaceTracking.Behaviors"
    xmlns:helpers="using:VRCFaceTracking.Helpers"
    behaviors:NavigationViewHeaderBehavior.HeaderMode="Never"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:EnumToStringConverter x:Key="EnumToStringConverter" />
        <helpers:BooleanToVisibilityConverter x:Key="BoolVisConverter" />

        <DataTemplate x:Key="ItemTemplate" x:DataType="models:InstallableTrackingModule">
            <Grid Height="60">
                <StackPanel Margin="{StaticResource SmallLeftMargin}" VerticalAlignment="Center">
                    <TextBlock Text="{x:Bind ModuleName}" Style="{StaticResource ListTitleStyle}" />
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <TextBlock Text="{x:Bind AuthorName}" Style="{StaticResource ListSubTitleStyle}" />
                        <TextBlock Text="{x:Bind InstallationState, Converter={StaticResource EnumToStringConverter}, Mode=OneWay}" Style="{StaticResource ListSubTitleStyle}" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="DetailsTemplate" x:DataType="models:InstallableTrackingModule">
            <views:ModuleRegistryDetailControl ListDetailsMenuItem="{x:Bind}" />
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0, 0, 0, 10" Padding="5" CornerRadius="6" BorderThickness="1" Background="{ThemeResource LayerFillColorDefaultBrush}">
            <StackPanel Padding="10, 10, 0, 10" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Uid="InstallCustomModuleButton" Click="InstallCustomModule_OnClick" Height="36">
                    <FontIcon FontSize="20" Glyph="&#xF8AA;" />
                </Button>
                <TextBlock x:Name="CustomInstallStatus" x:Uid="InstallCustomModuleHint" TextWrapping="Wrap" VerticalAlignment="Center" Style="{ThemeResource DetailBodyBaseMediumStyle}" Padding="20, 0, 0, 0" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" HorizontalAlignment="Stretch" Padding="5" Margin="0, 0, 0, 10" CornerRadius="6" BorderThickness="1" Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320" MinWidth="220" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock x:Uid="ModuleRegistryHeader" VerticalAlignment="Center" Margin="12,0,0,0" Style="{StaticResource ListTitleStyle}" />
                    <ListView Grid.Row="1"
                              ItemsSource="{x:Bind ViewModel.ModuleInfos}"
                              ItemTemplate="{StaticResource ItemTemplate}"
                              SelectedItem="{x:Bind ViewModel.Selected, Mode=TwoWay}"
                              SelectionMode="Single"
                              IsItemClickEnabled="False" />
                </Grid>
                <Border Grid.Column="1" BorderThickness="1,0,0,0" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" Padding="12,0,0,0">
                    <Grid>
                        <ContentControl Content="{x:Bind ViewModel.Selected, Mode=OneWay}"
                                        ContentTemplate="{StaticResource DetailsTemplate}"
                                        HorizontalContentAlignment="Stretch"
                                        VerticalContentAlignment="Stretch"
                                        Visibility="{x:Bind ViewModel.HasSelection, Mode=OneWay, Converter={StaticResource BoolVisConverter}}" />
                        <TextBlock x:Uid="ModuleRegistry_NoSelection"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   TextAlignment="Center"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   Visibility="{x:Bind ViewModel.HasSelection, Mode=OneWay, Converter={StaticResource BoolVisConverter}, ConverterParameter=Inverse}" />
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Page>
